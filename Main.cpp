#include "PA5.h"

int main() {
	runMusicManager();

	// Song tempSong("all too well", "taylor swift", "pop", 5);
	// Song tempSong2("Mirrorball", "Taylor Swift", "Pop", 4);
	// SongLibrary lib;
	// lib.performAddSong(tempSong);
	// lib.performAddSong(tempSong2);
	// lib.displayLibrary();

	return 0;
}
